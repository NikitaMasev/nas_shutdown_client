void main() {
/*  final portNas = int.parse(Platform.environment['PORT_NAS']!);
  final portIot = int.parse(Platform.environment['PORT_IOT']!);
  final ipNas = Platform.environment['IP_NAS']!;
  final usernameNas = Platform.environment['USERNAME_NAS']!;
  final passwordNas = Platform.environment['PASSWORD_NAS']!;*/
}
